Task class:
SECURITY_GOVERNANCE_IMPLEMENTATION + GITHUB_SECRET_LEAK_AUDIT + PREVENTION_ROLLOUT

Mission:
Audit in-scope GitHub organization repositories for leaked secrets without disclosing values, install reusable prevention controls, and store redacted receipts in the canonical control-spine repository.

Canonical repository:
arwoxbx24/agenthub-control-spine

Forbidden default owner:
arwoxb24

Rules:
- Never print, store, or commit secret values, tokens, private keys, passwords, `.env` contents, or raw scanner matches.
- Use redacted scanner output only.
- Treat confirmed exposed credentials as compromised until rotated or revoked.
- Do not rewrite history, delete repos, force-push, or rotate third-party secrets without an explicit incident task and service-owner authority.
- YouTrack remains the task system. GitHub is artifact storage and repository protection surface.

Execution:
1. Reuse or create one controlling task.
2. Read `AGENTS.md`, `INDEX.md`, and latest handoff from the canonical control-spine repo.
3. Inventory in-scope org repos and permissions.
4. Check GitHub native secret protection, push protection, rulesets, and branch protection availability.
5. Run redacted history scan with a pinned scanner version where feasible.
6. Create one bounded incident child for redacted findings requiring rotation/revocation review.
7. Add or update policy, runbook, workflow template, scanner config, local hook templates, schema, prompt, receipt, and `INDEX.md`.
8. Validate changed files with redacted secret/raw-link scan.
9. Open and merge a PR only if validation passes and no secret values are present.

Typed blockers:
- BLOCKER_GITHUB_AUTH_MISSING
- BLOCKER_ORG_ACCESS_MISSING
- BLOCKER_GITHUB_ADMIN_PERMISSION_MISSING
- BLOCKER_GITHUB_SECRET_PROTECTION_UNAVAILABLE
- BLOCKER_RULESET_PERMISSION_MISSING
- BLOCKER_SCANNER_INSTALL_UNAVAILABLE
- BLOCKER_REPO_TOO_LARGE_FOR_CURRENT_RUN
- BLOCKER_CONFIRMED_SECRET_ROTATION_REQUIRED
- BLOCKER_PERSONAL_OWNER_NOT_AUTHORIZED

Final:
Short Russian only. Include coverage, receipt path, and remaining typed blockers. Never include secret values.
